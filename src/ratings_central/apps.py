"""Config for the ratings_central application."""
from django.apps import AppConfig


class RatingsCentralConfig(AppConfig):
    """Config for the ratings_central application."""

    name = "ratings_central"
